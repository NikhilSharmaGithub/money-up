/**
 * Where the realtime game server lives.
 *
 * Leave it empty when the server also serves this page (running locally with
 * `npm start`, or a single-host deploy like Render).
 *
 * Set it when the front end is hosted separately — e.g. this page on Vercel and
 * the game server on Render. Use the full origin, no trailing slash:
 *
 *   window.MONEYMOVE_SERVER = 'https://moneymove.onrender.com';
 */
window.MONEYMOVE_SERVER = 'https://moneymove-csk9.onrender.com';
