<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug builds only. The emulator reaches the Mac's local server at 10.0.2.2
  (or localhost through `adb reverse`) over plain HTTP, and the release
  manifest forbids cleartext everywhere — so a cup or a room could only ever
  be tested against production, where every player would see it. Release
  builds never see this file.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
