org.gradle.jvmargs=-Xmx2048m
android.useAndroidX=true
android.nonTransitiveRClass=true

# The AdMob application id for the Android app — AdMob's Apps page, the one
# with the "~" in it, not a unit id with a "/". Not a secret: it ships inside
# the app. Uncomment and paste it here, or put the same line in
# local.properties or ~/.gradle/gradle.properties, or export ADMOB_APP_ID.
# Left out, the build uses Google's sample app id and says so; it never
# crashes over it. The unit ids are not here at all — the server hands them
# out, from the admin desk, so they change without a new build.
ADMOB_APP_ID=ca-app-pub-1179201999959612~9068269365
